@using Web_GiupViecNha.Areas.Admin.Models;
   @{ ViewBag.Title = "Quản lý cộng tác viên";
    Layout = "~/Areas/Admin/Views/Shared/_MainLayOut.cshtml";
}
@helper Truncate(string input, int length)
{
    if (input.Length < length)
    {
        @input

    }
    else
    {
        @input.Substring(0, length) <text>...</text>
    }
}



<h3 class="title-5 m-b-35">Danh sách Cộng tác viên</h3>
<div class="table-data__tool">
    <div class="table-data__tool-left">
        <div class="rs-select2--light rs-select2--md">
            <select class="js-select2" name="property">
                <option selected="selected">Tất cả</option>
                <option value="">Option 1</option>
                <option value="">Option 2</option>
            </select>
            <div class="dropDownSelect2"></div>
        </div>
 
    
    </div>
    <div class="table-data__tool-left">

        <input class="au-input au-input--xl" type="text" id="txtSearch" placeholder="Tên cộng tác viên" />
        <button class="btn btn-primary" id="btnSearch">
            <i class="zmdi zmdi-search"></i>
        </button>

    </div>
    <div class="table-data__tool-right">
        <button onclick="location.href='@Url.Action("DonDKCTV","CongTacVien")'" class="au-btn au-btn-icon  au-btn--green au-btn--small">
            <i class="zmdi zmdi-plus"></i>Đơn đăng ký CTV
        </button>

     
    </div>
</div>
<div class="table-responsive table-responsive-data2">
    <table class="table table-data2">
        <thead>
            <tr>
              
                <th>Mã CTV</th>
                <th>Tên CTV</th>
                <th>Email</th>
                <th>Ngày vào làm</th>
                <th>Trình độ</th>
                <th>Kinh nghiệm</th>
                <th>Điểm đánh giá</th>
                <th>Đơn đăng ký</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (CongTacVien ctv in ViewData["dsctv"] as List<CongTacVien>)
            {
                <tr class="tr-shadow">
                
                    <td>@ctv.MaCTV</td>
                    <td class="desc">
                        <span>@ctv.TenCTV</span>
                    </td>
              
                    <td>@ctv.Email</td>
                    <td>@ctv.NgayVaoLam</td>
                    <td>@ctv.TrinhDo</td>
                    <td>@ctv.KinhNghiem</td>
                    <td>@ctv.DiemDanhGia</td>
                    <td>@ctv.MaDonDKCTV</td>
                    <td>
                        <div class="table-data-feature">


                            <button class="item" data-toggle="tooltip" onclick="location.href='@Url.Action("Delete", "CongTacVien", new { id = ctv.MaCTV.Trim() })'" data-placement="top" title="Delete">
                                <i class="zmdi zmdi-delete"></i>
                            </button>
                            <button class="item" data-toggle="tooltip" data-placement="top" title="Edit">
                                @*<a href="@Url.Action("ThongTinChiTietDV", "DichVu", new { madv = dv.MaDichVu.Trim()})"> </a>*@
                                <i class="zmdi zmdi-edit"></i>
                            </button>
                        </div>
                    </td>
                </tr>
                <tr class="spacer"></tr>

            }


        </tbody>
    </table>
</div>
